# Inkspell《墨法奇书》首个可玩验证版模块设计

文档类型：模块设计

来源 GDD：`GDD.md`

## 当前工程事实

- `Assets/Game/Scripts/Runtime/HotUpdateRegistrar.cs` 是当前产品组合根：已注册框架提供的配置、本地存档、时间、UI、场景、输入、资源、音频等机制能力，但尚未注册 Inkspell 产品业务 System 或 Store。
- `LocalFileSaveSystem`、`JsonSerializeSystem` 和 `NoEncryptionSystem` 已存在；产品 Store 仍需在组合根中明确声明持久化边界，非持久战斗状态不进入本地存档。
- `TimeSystem` 已注册，可作为在线生成计时和离线时长计算的统一时间机制；时间异常的产品处理仍归法术生成模块。
- Luban 当前已有 `UIWindow`、`Language`、法术资产、合成、养成、敌人、关卡战斗、战斗规则与关卡推进作者源及生成表；法术生成配置尚未实施。各业务 System 直接通过框架 `IConfigSystem` 读取所属配置，不增设项目业务配置聚合模块。
- 法术资产的持久 Store/唯一 System、法术合成的 System/Procedure、法术养成的无 Store System、自动战斗的非持久 Store/System/Procedure，以及关卡推进的持久 Store/System/Procedure 已存在；它们的组合根注册与三个持久化边界仍按 M999 延迟收敛。
- 现有 `Localization` 与 `UI` 目录只是框架机制的 Luban 适配位置，不拥有 Inkspell 业务决策，不列为产品业务模块。

## 业务决策归属

| 业务决策、状态或不变量 | GDD 来源 | 归属模块 |
|---|---|---|
| 每张法术是具有唯一标识、种类、阶级、等级、位置与锁定状态的独立实例 | §7.1、§14.1 | 法术资产 |
| 同一法术实例只能位于合成区或一个装备槽，不能重复占位 | §7.4、§12.1 | 法术资产 |
| 合成区容量、未装备法术占用与自动稳定排列 | §9.1 | 法术资产 |
| 法术锁定状态随实例保存，移动或装备不自动解除 | §9.2 | 法术资产 |
| 四个装备槽可空、允许同名重复装备，替换时新旧法术原子交换位置 | §7.4、§12.1 | 法术资产 |
| 新进度初始拥有一张一阶 1 级火球，位于第一个装备槽，其余法术资产为空 | §4.1、§5、§20.2 | 法术资产 |
| 合成必须验证两个不同、同阶、未锁定、未装备、非最高阶且均为 1 级的实例 | §10.1、§10.4 | 法术合成 |
| 合法合成按输入阶级的独立固定成功率直接结算，无预览、保底或历史修正 | §10.2、§10.3 | 法术合成 |
| 合成成功原子消耗两张输入并产生随机高一阶、1 级法术 | §10.2 | 法术合成 |
| 合成失败原子消耗两张输入并按阶级发放魔法墨水 | §10.2、§11.1 | 法术合成 |
| 非法合成不进入随机判定、不消耗任何状态，并返回可区分的业务失败语义 | §10.1、§10.4、§18 | 法术合成 |
| 魔法墨水是唯一培养资源，余额与法术实例共享原子修改边界，数量跨启动保留且不得低于零 | §11.1、§14.1 | 法术资产 |
| 升级费用由法术阶级和当前等级决定，仅在墨水足够且未满级时提交 | §11.2 | 法术养成 |
| 单件升级必须原子扣除墨水并提升指定法术等级，不改变其他实例属性 | §11.2 | 法术养成 |
| 墨水不足或法术已满级时不修改任何状态，并提供对应失败事实 | §11.2、§18 | 法术养成 |
| 四种法术首版均为 5 级上限，共用主要数值倍率曲线，成本按阶级与当前等级确定 | §11.2 | 法术养成 |
| 在线每个生成周期按当前间隔产生一张随机一阶法术并计入待领取 | §8.1 | 法术生成 |
| 待领取不受合成区满格阻断，只在累计上限内增长，不丢失有效产出 | §8.1、§8.2、§8.3 | 法术生成 |
| 合成区有空位时，待领取法术自动移交至法术资产，待领取不允许手动操作 | §8.2 | 法术生成 |
| 离线不模拟战斗，只按合法离线时长、上限与离线期间有效生成间隔结算法术 | §6.4、§8.3、§14.2 | 法术生成 |
| 负离线时长不产生负收益，可结算时长不超过配置上限 | §8.3、§18 | 法术生成 |
| 生成间隔只由已解锁最高关卡进度决定，不低于配置最小值 | §4.2、§8.4 | 法术生成 |
| 首版离线上限为 8 小时，最小间隔为 45 秒，第 1—10 关间隔从 180 秒递减至 45 秒，四种开放一阶法术等权生成 | §8.4 | 法术生成 |
| 当前关卡是已到达的最高关，只能按固定顺序推进，不可选关、回退或跳关 | §13.1、§14.1 | 关卡推进 |
| 普通关胜利提交下一关；最大关胜利不再增长进度并重复当前关；胜利后停顿 1 秒、失败后停顿 2 秒 | §4.2、§4.3、§6.3、§17.4 | 关卡推进 |
| 每次挑战必须从当前关起点开始，胜利、失败或重新进入均由关卡推进发起下一次有界挑战 | §6.3、§14.2 | 关卡推进 |
| 关卡挑战按配置编队生成敌人，敌人沿单路径移动并在抵达后攻击魔法书 | §6.1、§13 | 自动战斗 |
| 装备槽按各自冷却自动施法，默认选择距魔法书最近的合法目标 | §6.1、§6.2 | 自动战斗 |
| 火球、雷链、冰环和护盾分别执行单体伤害、连锁伤害、范围伤害减速和魔法书临时护盾规则 | §7.2 | 自动战斗 |
| 没有合法目标时不施法；装备或升级只影响下一次合法施法，不追溯改写已发生攻击 | §6.2、§6.4、§12.2 | 自动战斗 |
| 战斗命中、最后敌人死亡、魔法书死亡和换装/升级的同时事件按固定优先级结算 | §14.3 | 自动战斗 |
| 挑战胜利是消灭全部敌人；有存活敌人时魔法书生命归零是失败 | §6.3、§14.3 | 自动战斗 |
| 失败后清除敌人与全部临时效果、恢复魔法书初始生命，战斗状态不跨重试或启动保留 | §6.3、§14.2 | 自动战斗 |
| 生成、合成、升级和换装与战斗并行，不暂停敌人移动、攻击或自动施法 | §6.4 | 自动战斗 |

## 模块总览

| 模块 | 长期业务能力 | 权威状态 | 稳定接口 | 依赖 |
|---|---|---|---|---|
| 法术资产 | 维持玩家法术实例与魔法墨水的唯一权威资产边界 | 全部有效法术实例、位置、装备槽映射、锁定状态、唯一标识分配状态、魔法墨水 | 资产查询、空位与自动排列视图、接收生成法术、锁定、装备/替换、经授权的合成或升级原子变更 | `IConfigSystem` |
| 法术合成 | 对一次玩家主动二合进行合法性判定、随机结算与完整提交 | 无 | 发起二合并返回成功产物、失败墨水或未消耗的失败事实 | 法术资产、`IConfigSystem` |
| 法术养成 | 解释魔法墨水与法术等级规则，对一张指定法术完成有成本的等级成长 | 无（魔法墨水与法术等级均归法术资产） | 升级信息查询、发起单件升级并返回数值变化或未消耗的失败事实 | 法术资产、`IConfigSystem` |
| 法术生成 | 按在线/离线时间生成一阶法术，管理待领取并在有空位时自动移交 | 待领取法术、当前生成周期进度、生效间隔、离线计算起点 | 待领取数量查询、在线推进、进入后台记录、返回时离线结算、空位自动补入 | 法术资产、关卡推进、`ITimeSystem`、`IConfigSystem` |
| 关卡推进 | 维持玩家当前最高关卡，按挑战结果进入下一关或重试/重复当前关 | 当前最高关卡 | 当前关卡查询、启动当前关挑战、提交挑战胜负并发布进度事实 | 自动战斗、`IConfigSystem` |
| 自动战斗 | 运行一次从关卡起点到胜利或失败的有界自动挑战 | 当次挑战的魔法书生命/护盾、敌人、弹道与效果、槽位冷却、结算状态 | 开始指定关卡的新挑战、推进战斗、读取当次战斗状态并产生唯一胜负结果 | 法术资产、`IConfigSystem` |

## 法术资产

### 负责

- 拥有玩家全部当前有效法术实例、魔法墨水余额和唯一标识分配状态的权威表示。
- 维持合成区与装备槽之间互斥的位置不变量，以及装备槽可空、单槽最多一个实例的不变量。
- 维持合成区容量；对未装备法术按 Luban 配置显示优先级与唯一标识生成稳定显示顺序，不持久化合成格显示索引。
- 完成锁定切换、装备和原子替换，并在空槽装备或其他变更释放合成区空位后发布稳定的容量变化事实。
- 维持魔法墨水不得低于零的状态不变量，为合成与养成提供同一 Store 内的法术/墨水原子变更。
- 为合成、养成与生成模块提供不暴露内部集合的语义更改能力，并且只在完整变更后发布业务事实。

### 明确不负责

- 不判定二合成功率、随机产物或失败墨水；这些归法术合成。
- 不解释升级成本、等级上限或墨水是否足够；这些业务判定归法术养成。
- 不计时产生法术，不拥有待领取队列；这些归法术生成。
- 不执行法术战斗行为、冷却或战场表现；这些归自动战斗。

### 权威状态与不变量

- 持久状态：法术实例种类、阶级、等级、位置、装备槽编号、锁定状态、唯一标识及其后续分配状态、魔法墨水；四个装备槽映射由这些实例的位置唯一导出，不允许两份可独立修改的真相。
- 合成区中的实例数不超过配置容量；已装备实例不占合成区容量。
- 装备替换在一次提交中使新法术进入目标槽、旧法术回到合成区，中间不暴露超容量或重复占位状态。
- 初始状态只创建 GDD 指定的一阶、1 级火球并放入第一个装备槽；其余槽位和合成区为空。
- 魔法墨水只能通过经授权的合成失败发放或单件升级扣除而变化，任何提交都不得将余额降至零以下。

### 稳定语义接口

- 查询指定法术、当前装备与魔法墨水，获取未装备法术的稳定排列结果和剩余容量；返回只读业务数据，外部不直接修改 Store Data。
- 接收法术生成模块移交的一阶法术，仅在有合成区容量时创建唯一实例。
- 处理锁定切换与从合成区到装备槽的装备/替换，以明确的成功或普通业务失败语义拒绝无效操作。
- 接受法术合成模块的一次经判定结算，在同一 Store 内原子消耗输入并创建成功产物或发放失败墨水；接受法术养成模块的经判定升级，原子扣除墨水并提升等级。

### 跨模块协作与事务编排

- 法术资产只对自身单 Store 内的位置、装备、锁定、实例与墨水变更原子提交；不编排合成随机结算或解释养成成本。
- 容量变更在资产状态完整提交后发布；法术生成模块作为唯一补入编排者消费该事实，资产模块不反向依赖生成模块。

### JulyArch 角色规划

- 一个项目业务 Store：拥有法术实例、装备与魔法墨水的统一持久状态，所有修改通过业务语义方法完成，一致变更后再发布事件和 Dirty。
- 一个项目业务 System：提供资产查询、锁定和装备的长期稳定入口，并为授权模块提供受控原子变更。
- 当前不需要 Procedure；锁定、装备和单 Store 变更都是同步有界动作。

### 已确认扩展

无。仅覆盖 GDD 已承诺的四种法术、三个阶级、四个装备槽与 6×4 合成区；不为未确认的更多槽位、配装或图鉴预建职责。

## 法术合成

### 负责

- 对一次二合请求执行唯一的合法性判定，包括实例、阶级、锁定、位置、最高阶和已培养限制。
- 按输入阶级的 Luban 配置对合法请求进行一次独立随机判定，成功时抽取高一阶法术，失败时确定墨水奖励。
- 作为整个合成事务的唯一编排者，确保两张法术只在结果已确定时被消耗，失败墨水与消耗对外只显示为一次完整提交。
- 产生可区分的成功、转化为墨水或未消耗失败业务事实，供主界面播放反馈。

### 明确不负责

- 不拥有法术实例、装备槽、锁定状态或魔法墨水；这些统一归法术资产。
- 不判定法术升级成本或上限；这些归法术养成。
- 不负责合成动画、Tip 样式、拖拽命中或结果播放时序；这些属于 View 表现。

### 权威状态与不变量

无独立持久或长期运行时状态。每次合法请求的输入、随机结果与提交进度只属于当次有界流程；该模块不保存历史成功率、失败累计或保底状态。

### 稳定语义接口

- 接收两个玩家选定的法术实例标识，执行完整二合，向调用方返回是否已成功提交。
- 对普通业务失败提供稳定可区分的事实，如锁定、不同阶、已装备、最高阶或已培养；失败分支自身记录必要上下文，调用方不重复实现规则。

### 跨模块协作与事务编排

- 法术合成是二合事务的唯一编排者。它先通过法术资产读取业务事实并完成全部可恢复失败判定，然后请求法术资产在同一 Store 提交“消耗输入+创建成功产物”或“消耗输入+发放失败墨水”。
- 法术资产完整提交后才发布最终合成结果和容量变化事实；法术生成随后消费容量事实，保持“合成结果完成后再自动补入”。

### JulyArch 角色规划

- 一个项目业务 System：作为二合的长期稳定业务入口。
- 无 Store；模块没有 GDD 允许的历史或保底状态。
- 一次合成使用由 System 创建和运行的 Procedure，封装合法性判定、随机结算、法术资产单 Store 完整提交与失败语义。

### 已确认扩展

无。GDD 明确排除保底、失败累计、指定产物、合成预览、撤销、自动合成和批量合成。

## 法术养成

### 负责

- 拥有单件法术培养的成本、上限、成长与成败决策，使用法术资产的权威墨水和法术状态。
- 解释 Luban 中由法术阶级、当前等级、等级上限和成长值共同决定的升级规则。
- 解释首版 1—5 级成长曲线，将配置倍率应用于各法术当前阶级的基础主要数值；成本按阶级与当前等级区分，四种法术保留独立调参记录。
- 对一张指定法术执行单件升级，确保墨水扣除与法术等级变更对外只显示为一次完整提交。
- 产生墨水、等级和主要数值变化的业务事实，供法术资产重排、战斗下次施法和 View 反馈消费。

### 明确不负责

- 不拥有法术实例的种类、阶级、等级、位置、锁定状态或魔法墨水余额；这些统一归法术资产。
- 不使用墨水影响合成成功率，不处理培养返还、重置或降级。
- 不执行法术战斗效果，不追溯改写已发起的施法。

### 权威状态与不变量

无独立持久或长期运行时状态。魔法墨水与法术等级位于法术资产的同一 Store；法术养成独立拥有升级判定，不复制这些状态。首版合法等级范围为 1—5，成功升级只增加一级；升级失败不修改任何资产，升级成功不改变法术种类、阶级、位置、槽位或锁定状态。

### 稳定语义接口

- 查询当前墨水和指定法术的升级所需成本、上限与成长后数值。
- 对指定法术发起升级，成功时返回已完整提交的资源、等级和主要数值变化，失败时返回墨水不足或已满级事实且不消耗状态。

### 跨模块协作与事务编排

- 法术养成是单件升级事务的唯一编排者。它读取法术资产事实和 Luban 成本/成长，先完成全部可恢复失败判定，再请求法术资产在同一 Store 内原子扣除墨水并提升指定法术等级，完成后才发布升级结果。
- 合成失败中的墨水奖励数量和发放均由法术合成通过法术资产的原子结算完成；法术养成不介入合成事务，也不依赖法术合成。

### JulyArch 角色规划

- 无 Store；模块不复制魔法墨水或法术等级状态。
- 一个项目业务 System：提供升级信息查询与单件升级的长期稳定入口。
- 当前不需要 Procedure；所有可恢复失败在提交前完成判定，最终变更是法术资产单 Store 内的同步原子操作。

### 已确认扩展

无。GDD 明确排除培养返还、重置、降级和其他中间资源。

## 法术生成

### 负责

- 使用框架时间能力和 Luban 配置维持在线生成周期，每次到期从开放法术池按权重产生一阶法术。
- 拥有待领取法术，保证合成区满时只停止补入而不停止有效生成。
- 作为待领取自动补入的唯一编排者，在法术资产有空位时将待领取产物移交为真实法术实例。
- 记录进入后台或退出时间，再次进入时根据当时有效间隔、离线上限与合法时长计算新产物，先完整计入待领取并执行可完成的自动补入，再产生离线收益事实。
- 根据关卡推进公布的当前最高关，唯一解释该关对应的生成间隔并应用最小值边界。

### 明确不负责

- 不拥有已进入合成区或装备槽的法术实例；这些归法术资产。
- 不根据战斗过程、配装、合成或升级附加其他效率公式。
- 不进行离线战斗、关卡胜负或关卡进度提交。
- 不把待领取暴露为第二个可操作背包。

### 权威状态与不变量

- 持久状态：待领取法术、当前生成周期所需时间状态、当前生效生成间隔、最后进入后台或退出的时间。
- 待领取只能由成功的在线/离线生成增加，或在法术资产确认接收后减少；不因合成区无容量而删除。
- 离线可计入时长不小于零、不大于配置上限；离线结算不改变任何战斗状态。
- 当前间隔只是关卡进度配置的解释结果，不由其他模块直接写入，且不小于配置最小间隔。

### 稳定语义接口

- 查询待领取数量、当前生成进度和生效间隔。
- 在前台时按实际经过时间推进生成，在进入后台/退出时记录起点，在回到前台/启动时结算一次有上限的离线收益。
- 在自身生成或法术资产容量变化后尝试自动补入，并只在法术资产成功接收后提交待领取减少。

### 跨模块协作与事务编排

- 法术生成是“待领取→合成区”事务的唯一编排者。它查询法术资产容量，请求创建一个法术实例，只在法术资产确认后才从待领取移除对应产物。
- 法术生成读取关卡推进发布的进度事实并更新生效间隔；关卡推进不直接写生成状态，因而不形成循环依赖。
- 离线结算先完整增加待领取并执行可完成的自动补入，再产生包含合法时长、产生数和补入数的可展示收益事实；View 只在提交完成后展示，不自行计算、修改或回滚收益。

### JulyArch 角色规划

- 一个项目业务 Store：拥有待领取、生成周期和离线时间的持久状态。
- 一个项目业务 System：作为在线推进、离线结算与自动补入的长期业务能力，使用框架时间和配置能力。
- 一次离线结算及其后紧随的自动补入使用由 System 创建和运行的 Procedure；普通在线周期推进不需要 Procedure。

### 已确认扩展

无。仅包含 GDD 当前的一阶生成、待领取和有上限离线收益；不预建体力、多类产出或运营挂机系统。

## 关卡推进

### 负责

- 拥有玩家已到达的当前最高关卡，初始为第 1 关且不超过 Luban 配置的最大关。
- 作为连续自动推关的唯一编排者，对当前关发起一次有界自动战斗，消费其唯一胜负结果，然后按胜利 1 秒、失败 2 秒的停顿规则发起下一次。
- 胜利时在未达最大关的情况下将进度提交为下一关；失败或最大关胜利时保持进度并重试/重复当前关。
- 在进度确实增长后发布关卡进度事实，供法术生成更新挂机效率与 View 更新关卡显示。

### 明确不负责

- 不模拟敌人、魔法书、法术冷却、弹道或效果；这些归自动战斗。
- 不解释关卡进度对生成间隔的具体数值影响；这些归法术生成。
- 不保存关内已消灭敌人、当前生命或其他临时挑战状态。
- 不提供选关、回退、跳关或手动开战。

### 权威状态与不变量

- 持久状态：当前最高关卡。
- 关卡从第 1 关开始，只能在当前关胜利后增加一关，并且不超过配置最大关。
- 一次挑战只能提交一个最终结果；进度变更后才发布进度事实。

### 稳定语义接口

- 查询当前最高关卡，供生成间隔、存档显示和主界面使用。
- 启动当前关的一次新挑战，对其胜利或失败进行唯一提交，再产生关卡增长或原关重试的业务事实。

### 跨模块协作与事务编排

- 关卡推进是“开始挑战→等待胜负→提交进度→开始下一次”的唯一编排者。它把当前关标识与关卡配置交给自动战斗，自动战斗只返回当次结果，不反向修改关卡 Store。
- 进度增长事实由法术生成消费；关卡推进不调用法术生成，去除返向依赖。

### JulyArch 角色规划

- 一个项目业务 Store：拥有当前最高关卡的持久状态及其单向推进语义。
- 一个项目业务 System：作为连续推关的长期业务能力和 View 的稳定关卡查询入口。
- 每次关卡挑战使用由 System 创建和运行的 Procedure，封装一次从起点到胜负、结果提交和停顿后衔接的有界流程。

### 已确认扩展

无。仅支持 GDD 已承诺的十个顺序关卡和最大关重复挑战；不预建选关、章节、体力或手动开战。

## 自动战斗

### 负责

- 从关卡推进接收一个指定关卡的新挑战，按 Luban 编队、敌人、魔法书与时序配置初始化临时战场。
- 拥有魔法书生命与护盾、敌人生命/位置/状态、法术冷却、已发出攻击与效果等当次挑战权威状态。
- 按配置时机生成敌人并推进单路径移动、接敌后攻击、护盾先于生命吸收伤害、敌人死亡与临时减速。
- 对每个装备槽独立推进冷却，在满足时机时重新读取当前装备法术及最新等级，以最近合法目标执行火球、雷链、冰环或护盾。
- 按 GDD 的同时结算优先级将一次挑战最终化为唯一胜利或失败结果，不自行更改关卡进度。

### 明确不负责

- 不拥有玩家持久法术、装备关系或等级；它在每次施法发起时读取法术资产的最新事实。
- 不修改当前最高关卡、生成效率、待领取或魔法墨水。
- 不在后台或离线时继续模拟战斗。
- 不决定战场美术、弹道动画、受击表现或 UI 反馈。

### 权威状态与不变量

- 非持久状态：当前挑战标识与关卡、魔法书当前生命/护盾、场上敌人及其生命/位置/效果、出生进度、槽位冷却、已发攻击与当次结果。
- 新挑战始终清除上次敌人、临时效果、弹道和冷却，恢复魔法书初始生命并从关卡编队起点开始。
- 已发起的攻击保留发起时所需的战斗参数；之后的装备或等级变化只影响下一次合法施法。
- 已死亡的最后敌人不再在同一结算时机攻击；关卡已无存活敌人时胜利优先于魔法书死亡判定。

### 稳定语义接口

- 对指定关卡开始一次全新挑战，在前台时推进当次战斗，并最终只产生一个胜利或失败结果。
- 查询当次战斗的魔法书、敌人、冷却、法术效果和挑战结果业务数据，供 WindowData 组装后作为战场 GameView 显示输入；View 不直接修改 Store。

### 跨模块协作与事务编排

- 自动战斗只编排单次挑战内部的敌人、施法与结算步骤。关卡推进是跨挑战循环的唯一编排者，因此自动战斗不读取或修改关卡 Store，也不自行开始下一关。
- 每次到达合法施法时机时，自动战斗通过法术资产的只读稳定能力获取当前槽位法术，不缓存为可被换装/升级追溯修改的第二份法术状态。

### JulyArch 角色规划

- 一个项目业务 Store：拥有当次挑战的全部权威运行时状态，但不参与持久化。
- 一个项目业务 System：作为运行指定关卡单次自动挑战的长期能力，并在每帧或统一时间推进中调用 Store 语义变更。
- 每次挑战使用由 System 创建和运行的 Procedure，封装从初始化到唯一胜负的有界生命周期；产品 GameView 不直接构造或运行该 Procedure。

### 已确认扩展

无。仅支持 GDD 已承诺的单路线、四种法术、四种敌人与胜负/重试闭环；不预建手动施法、多路线、远程敌人、免疫或其他未承诺战斗能力。

## 全局依赖与集成关系

```text
主界面 Window / 战场 GameView
  → 法术资产 / 法术合成 / 法术养成 / 法术生成 / 关卡推进

法术合成 → 法术资产

法术养成 → 法术资产

法术生成 → 法术资产
             → 关卡推进

关卡推进 → 自动战斗

自动战斗 → 法术资产
```

- 上述业务依赖是单向无环的。容量变化、关卡进度、法术等级/装备变化等反向通知通过只读查询或最小业务事实完成，不让被依赖模块调用上层模块。
- 合成、升级、待领取补入和关卡挑战分别只有一个编排者；Window/GameView 只提交玩家意图和渲染 WindowData，不掌握跨模块调用顺序。
- 法术资产、法术生成和关卡推进 Store 参与本地持久化；自动战斗 Store 明确不参与，法术合成与法术养成没有 Store。组合根必须在 `ArchContext` 初始化前完成 Store、System 和持久化声明，依赖已有的 `LocalFileSaveSystem`、`JsonSerializeSystem` 与 `NoEncryptionSystem`。
- 法术、合成/墨水、生成、关卡/敌人与战斗数值属于 Luban 静态策划事实；各业务 System 直接通过现有 `IConfigSystem` 读取本模块所需表，不在 Store 中复制配置，不增设 `ConfigSystem`/`ContentSystem` 类产品模块。
- 存档恢复后，由法术生成先独立完成离线结算与自动补入，再由 View 展示已提交结果；关闭展示不影响生成状态。战斗仍从持久化的当前关起点开始，且只有生成模块使用离线时间。
- 主界面的 WindowData 是唯一显示输入，首屏从各模块的只读能力组装展示数据，后续可按业务事实刷新；它不修改 Store、不导航、不运行 Procedure。拖拽影子、动画进度、按压与当前播放位置等保持在 Window/GameView 的纯表现状态中。
